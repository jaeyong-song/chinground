<%= stylesheet_link_tag "users" %>

<h3>나의 프로필 페이지입니다</h3>

<h3>친구(팔로우)</h3>

<% @user.followers.each do |user|%>
    <p><%= user.email %></p>
<% end %>

<h3>친구(팔로워)</h3>

<% @user.followees.each do |user|%>
    <p><%= user.email %></p>
<% end %>

<div class="table-area">
    <h3>참여한 게시물 내역</h3>
    
    <table class="table table-hover">
        <thead class="thead-light">
            <tr><th scope="col" class="ground_list">번호</th>
                <th scope="col" class="ground_list">제목</th>
                <th scope="col" class="ground_list">장소</th>
                <th scope="col" class="ground_list">작성자</th>
            </tr>
        </thead>
        <tbody>
        <% @joined.each do |article| %>
            <tr>
                <% if article.active %>
                    <td scope="row" class="active_list"><%= link_to article.id, "/article/show/#{article.id}" %></td>
                    <td  class="active_list"><%= link_to article.title, "/article/show/#{article.id}" %></td>
                    <td  class="active_list"><%= link_to article.place, "/article/show/#{article.id}" %></td>
                    <td  class="active_list"><%= link_to article.user.email, "/article/show/#{article.id}" %></td>
                <% else %>
                    <td scope="row" class="done_list"><%= link_to article.id, "/article/show/#{article.id}" %></td>
                    <td class="done_list"><%= link_to article.title, "/article/show/#{article.id}" %></td>
                    <td class="done_list"><%= link_to article.place, "/article/show/#{article.id}" %></td>
                    <td class="done_list"><%= link_to article.user.email, "/article/show/#{article.id}" %></td>
                <% end %>
            </tr>
        <% end %>
        </tbody>
    </table>
</div>