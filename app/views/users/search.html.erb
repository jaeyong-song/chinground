<!--[TODO] 나중에 user에 이름 콜룸 추가하고 수정할 것!!-->

<%= search_form_for @search, url: '/users/search', class: "form-group" do |f| %>
    <%= f.text_field :email_cont, class: "form-control", placeholder: "email...", autofocus: true %>
    <%= f.submit "검색", class: "btn" %>
<% end %>
<p>검색 결과를 보시려면 '검색'을 클릭해주세요^^</p>

<table class="table table-hover">
  <thead>
    <tr>
      <th scope="col">번호</th></th>
      <th scope="col">email</th>
      <th scope="col">친구?</th>
      <th scope="col">follow</th>
      <!--이것도 추후 수정 필요!!-->
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
    <tr>
      <th scope="row"><%= user.id %></th>
      <td><%= user.email %></td>
      <td><%#= user.name %></td>
      <td><%= link_to '친구되기', "/users/follow/#{user.id}", method: :post %></td>
      <td></td>
    </tr>
    <% end %>
  </tbody>
</table>